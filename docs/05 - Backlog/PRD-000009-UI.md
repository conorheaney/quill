# PRD-000009-UI

## Short Name

Expanded Theme Selector

## Goal

Let users choose among an expanded set of named built-in visual themes through a dropdown selector.

## Context

The current app supports only light/dark switching through the `CYCLE THEME` control. That interaction hides the available choices and does not scale as additional built-in themes are introduced, even though the active theme is already persisted.

## Scope

In:

- replace the cycle-only theme button with a clearly labelled dropdown selector
- retain the existing light and dark themes
- add at least two additional named built-in themes with meaningfully distinct visual treatment
- define the initial theme names, values, stylesheet boundaries, and default selection during `10 - Plan`
- keep theme choice persistence aligned with the existing saved-theme behavior
- update product and usage documentation for the expanded theme selector

Out:

- user-created or user-edited custom themes
- a broad visual redesign unrelated to theme selection
- low-level stylesheet refactoring that belongs in `PRD-000008-TECH`, except for minimal integration required to expose the planned themes

## Plan

- Define the concrete dropdown semantics, initial theme set, persistence and fallback behavior, stylesheet integration boundary, documentation updates, and regression checks during `10 - Plan`.

## Acceptance Criteria

- `AC-01`: The current cycle-only theme button is replaced by a labelled dropdown selector that exposes each supported theme by name.
- `AC-02`: The initial supported set retains Light and Dark and adds at least two additional named built-in themes with distinct visual treatment.
- `AC-03`: Selecting a theme applies it immediately, updates the visible selector value, and persists the selection across application restarts.
- `AC-04`: An invalid, missing, or unavailable saved theme falls back to the defined default without breaking startup or leaving multiple theme styles active.
- `AC-05`: Existing editor, preview, outline, pane, dialog, file, and dirty-state behavior remains unchanged apart from the theme-selection interaction.

## Verification

| Test Case | Criteria | Product Version | Status | Description | Evidence |
| --- | --- | --- | --- | --- | --- |
| `TC-01` | `AC-01` | `pending` | `planned` | Define the verification approach, exact theme names, product version, and evidence format during `10 - Plan`. | Not yet recorded. |

## Next Step

Move this item to `10 - Plan` and finalize the theme names, visual boundaries, selector behavior, persistence fallback, and regression-check approach.

## History

| Timestamp | Stage |
| --- | --- |
| 2026-07-12T02:44:51.7632659Z | Backlog |

## Audit

| Timestamp | Type | Detail |
| --- | --- | --- |
| 2026-07-12T02:44:51.7632659Z | Scope discovery | Added as a backlog item for named predefined theme selection, separate from CSS refactoring and accessibility work. |
| 2026-07-12T02:44:51.7632659Z | State | Current state: backlogged and waiting to move into `10 - Plan`. |
| 2026-07-12T13:09:26.2378048Z | Consistency review | Reviewed during the workflow consistency sweep. No history backfill was required because this item has only reached `Backlog` and already had a captured UTC timestamp. |
| 2026-08-05T23:00:55.2143508Z | Requirement shaping | Refined the existing predefined-theme item to cover the requested expansion of built-in themes and replacement of the cycle-only control with a named dropdown. The initial set remains a planning decision; the backlog scope requires retaining Light and Dark plus at least two additional themes. |

