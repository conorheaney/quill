# PRD-000009-UI

## Short Name

Expanded Theme Selector

## Goal

Let users choose among an expanded set of named built-in visual themes through a dropdown selector.

## Context

The current app supports only light/dark switching through the `CYCLE THEME` control. That interaction hides the available choices and does not scale as additional built-in themes are introduced, even though the active theme is already persisted.

## Scope

In:

- replace the cycle-only theme button with a clearly labelled dropdown selector
- retain the existing light and dark themes
- add at least two additional named built-in themes with meaningfully distinct visual treatment
- define the initial theme names, values, stylesheet boundaries, and default selection during `10 - Plan`
- keep theme choice persistence aligned with the existing saved-theme behavior
- update product and usage documentation for the expanded theme selector

Out:

- user-created or user-edited custom themes
- a broad visual redesign unrelated to theme selection
- low-level stylesheet refactoring that belongs in `PRD-000008-TECH`, except for minimal integration required to expose the planned themes

## Plan

- Define the concrete dropdown semantics, initial theme set, persistence and fallback behavior, stylesheet integration boundary, documentation updates, and regression checks during `10 - Plan`.

Planning decisions:

- The initial named theme set is `Light`, `Dark`, `Sepia`, and `Nord`, with stable values `light`, `dark`, `sepia`, and `nord`. `Sepia` provides a warm paper-oriented treatment; `Nord` provides a cool blue-gray treatment distinct from both existing themes.
- The selector is a native labelled `<select>` control in the existing document-actions toolbar. Its label is programmatically associated with the control, the selected option is the current theme name, and changing the value applies the theme immediately without changing document content, focus state, or dirty state.
- The existing `quill-theme` local-storage key remains the persistence contract. The default is `dark`; a missing, malformed, or unsupported stored value is normalized to `dark`, applied, and persisted back as the valid fallback.
- Each supported value has one named stylesheet under `code/styles/themes/`, and the existing `data-quill-theme` activation boundary remains the mechanism for ensuring exactly one theme stylesheet is enabled at a time. Theme files own visual tokens only; structural/component rules remain in `code/styles/quill.css`.
- The implementation will update the runtime selector wiring, theme activation/validation, and the four theme stylesheets only as needed for this item. It will not perform general stylesheet restructuring or alter unrelated pane, editor, file, dialog, autosave, or dirty-state behavior.
- Product/usage guidance will replace the `CYCLE THEME` instruction with the named selector behavior in `code/quill.md` and the shipped getting-started text in `code/scripts/app-config.js`.

## Acceptance Criteria

- `AC-01`: The cycle-only theme control is replaced by a clearly labelled dropdown that lets the user choose a theme by name.
- `AC-02`: The dropdown offers exactly four named choices—Light, Dark, Sepia, and Nord—and each choice produces a visibly distinct appearance.
- `AC-03`: Choosing a theme changes the appearance immediately, shows the chosen name in the dropdown, and restores that choice after the app is closed and reopened.
- `AC-04`: When the app has no usable previous theme choice, it opens successfully using Dark as the default and shows only one active appearance.
- `AC-05`: Changing themes leaves the current document text, editor and preview content, outline selection, pane visibility, dialogs, file state, autosave setting, and dirty indicator unchanged.
- `AC-06`: All four theme choices are presented with the exact names Light, Dark, Sepia, and Nord, and each can be selected and displayed successfully.
- `AC-07`: The theme dropdown can be reached and operated with the keyboard, has a visible or announced label, and changing it does not mark the document as dirty or change unrelated app state.
- `AC-08`: The in-app guidance describes choosing a named theme from the dropdown and mentions Light, Dark, Sepia, and Nord; it does not instruct the user to cycle through themes.

## Verification

| Test Case | Criteria | Product Version | Status | Description | Evidence |
| --- | --- | --- | --- | --- | --- |
| `TC-01` | `AC-01` | `pending` | `planned` | Basic smoke test: launch Quill, confirm the cycle-only control has been replaced by a clearly labelled theme dropdown, open it, and confirm that theme choices are available by name. | Not yet recorded. |
| `TC-02` | `AC-02`, `AC-06` | `pending` | `planned` | Open the dropdown, confirm it contains exactly Light, Dark, Sepia, and Nord, select each choice in turn, and confirm the app visibly changes appearance each time. | Not yet recorded. |
| `TC-03` | `AC-03` | `pending` | `planned` | Select each named theme, confirm the appearance changes immediately and the selected name remains visible, close and reopen Quill, and confirm the last selected theme is restored. | Not yet recorded. |
| `TC-04` | `AC-04` | `pending` | `planned` | Close Quill, temporarily rename the webview folder, relaunch Quill with fresh storage, and confirm that it opens normally in Dark with one consistent appearance. Restore the backup folder after testing if required. `%LOCALAPPDATA%\quill.markdown.editor\EBWebView` | Not yet recorded. |
| `TC-05` | `AC-05`, `AC-07` | `pending` | `planned` | Reach the theme dropdown with the keyboard and select a different theme; before and after the change, compare document text, preview, outline, pane visibility, dialogs, file state, autosave setting, and dirty indicator. | Not yet recorded. |
| `TC-06` | `AC-08` | `pending` | `planned` | Read the in-app usage and getting-started guidance and confirm it explains named dropdown selection, lists all four themes, and contains no cycle-only instruction. | Not yet recorded. |

## Next Step

Begin implementation after the Plan gate is approved, following the finalized theme names, visual boundaries, selector behavior, persistence fallback, and regression-check approach.

Planning is finalized for the next gate. The next action is to promote the PRD to `Implement` after confirming the appended decisions and test cases, then implement the selector and four-theme contract within the stated stylesheet and documentation boundaries.

## History

| Timestamp | Stage |
| --- | --- |
| 2026-07-12T02:44:51.7632659Z | Backlog |
| 2026-08-08T08:33:11.3715766Z | Plan |
| 2026-08-08T09:00:44.0173750Z | Implement |

## Audit

| Timestamp | Type | Detail |
| --- | --- | --- |
| 2026-07-12T02:44:51.7632659Z | Scope discovery | Added as a backlog item for named predefined theme selection, separate from CSS refactoring and accessibility work. |
| 2026-07-12T02:44:51.7632659Z | State | Current state: backlogged and waiting to move into `10 - Plan`. |
| 2026-07-12T13:09:26.2378048Z | Consistency review | Reviewed during the workflow consistency sweep. No history backfill was required because this item has only reached `Backlog` and already had a captured UTC timestamp. |
| 2026-08-05T23:00:55.2143508Z | Requirement shaping | Refined the existing predefined-theme item to cover the requested expansion of built-in themes and replacement of the cycle-only control with a named dropdown. The initial set remains a planning decision; the backlog scope requires retaining Light and Dark plus at least two additional themes. |
| 2026-08-08T08:33:11.3715766Z | Promotion | Promoted from `Backlog` to `Plan` after workflow validation; planning now owns the concrete theme set, selector behavior, persistence fallback, stylesheet boundary, documentation updates, and regression-check approach. |
| 2026-08-08T08:36:03.9329263Z | Planning decision | Appended the initial four-theme set (`Light`, `Dark`, `Sepia`, `Nord`), selector and persistence contract, stylesheet ownership boundary, documentation targets, and six planned verification cases. Existing PRD content was left unchanged. |
| 2026-08-08T09:00:27.1394071Z | Promotion repair | Replaced the stale current-phase Next Step instruction with the Implement action so the Plan-to-Implement gate has one unambiguous next step. |
