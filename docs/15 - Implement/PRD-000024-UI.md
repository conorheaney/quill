# PRD-000024-UI

## Short Name

Compact Outline Pane

## Goal

Increase the number of headings visible in Quill's Outline pane by removing its redundant header and non-essential vertical spacing around Outline entries while preserving clear navigation.

## Context

The Outline pane currently spends vertical space on an `OUTLINE` header and on surrounding spacing for each entry. This reduces the amount of the document structure visible at once and forces users to scroll sooner when working with heading-rich Markdown files.

## Scope

In:

- remove the visible `OUTLINE` header from the Outline pane
- remove non-essential vertical margins and padding around individual Outline entries
- preserve clickable heading navigation, active-entry highlighting, heading-level indentation, text wrapping, and pane scrolling
- preserve an understandable empty state when a document has no headings

Out:

- changing which Markdown heading levels appear in the Outline
- changing heading parsing, navigation targets, or active-heading behavior
- redesigning the sidebar identity, document controls, or unrelated pane layouts
- removing readable line height or hierarchy indentation needed to distinguish entries

## Plan

1. Remove the `pane-header` element that labels the Outline from `code/html/outline-pane.html`, leaving the Outline navigation as the first content in the pane.
2. In `code/styles/quill.css`, give each `.outline-item` modest vertical padding with no horizontal padding, and remove the vertical gap from the `.outline` container so entries remain directly grouped. Preserve the existing heading-level `margin-left` indentation for H2 and H3 entries.
3. Keep the existing Outline entry font, line-height, wrapping, hover/focus/active treatment, and navigation event handling unchanged; do not compress typography to achieve the density improvement.
4. Keep the Outline empty-state message functional, removing only its non-essential surrounding spacing if needed to match the headerless compact layout.
5. Verify a heading-rich document and a document without headings in the supported desktop layout. Confirm the header is absent, more entries fit, hierarchy and long-entry wrapping remain clear, navigation and active highlighting still work, and the Outline scrolls when required.

## Acceptance Criteria

- AC-01: The Outline pane no longer displays the `OUTLINE` header, and the first Outline entry or empty state begins in the space previously occupied by the header without an unintended blank region.
- AC-02: Outline entries have modest vertical padding above and below their text, no horizontal padding, and no inter-entry gap; heading-level indentation remains the only intentional horizontal entry spacing, while the existing font and line height remain unchanged.
- AC-03: Clicking an Outline entry still navigates to the matching heading, and the active-entry highlight remains correct after navigation and scrolling.
- AC-04: Long entry text continues to wrap within the pane, and the Outline remains scrollable when its contents exceed the available height.
- AC-05: A document without headings still shows the existing empty-state message in the compact layout, and the sidebar's unrelated identity and controls are unchanged.

## Verification

| Test Case | Criteria | Product Version | Status | Description | Evidence |
| --- | --- | --- | --- | --- | --- |
| `TC-01` | `AC-01`, `AC-02`, `AC-05` | `pending` | `planned` | Compare the Outline pane with a heading-rich document and an empty document. Confirm the header is absent, entries begin at the compact top position, each entry has vertical text padding without horizontal padding, inter-entry gaps are removed, more entries fit, and the empty state remains present and readable. | Not yet recorded. |
| `TC-02` | `AC-03`, `AC-04` | `pending` | `planned` | Select entries at multiple heading levels, including a long wrapped entry, and confirm navigation, active highlighting, hierarchy indentation, wrapping, and scrolling continue to work. | Not yet recorded. |
| `TC-03` | `AC-01`, `AC-02`, `AC-03`, `AC-04`, `AC-05` | `pending` | `planned` | Run the focused Outline checks in the supported desktop layout and confirm no unrelated sidebar or pane regression. | Not yet recorded. |

## Next Step

The corrected packaged `1.0.8` Test candidate is ready for explicit `prd-promote` validation. Promote this item to `Test` before recording formal acceptance evidence.

## History

| Timestamp | Stage |
| --- | --- |
| 2026-08-07T15:08:16.9972788Z | Backlog |
| 2026-08-07T15:12:37.7790094Z | Plan |
| 2026-08-07T15:20:02.6399549Z | Implement |

## Audit

| Timestamp | Type | Detail |
| --- | --- | --- |
| 2026-08-07T15:08:16.9972788Z | Requirement shaping | Captured the request to remove the visible `OUTLINE` header and eliminate non-essential spacing around Outline entries so more headings fit vertically. Preserved navigation, active highlighting, hierarchy indentation, wrapping, scrolling, and the empty state as explicit constraints; excluded heading parsing and unrelated sidebar redesign. |
| 2026-08-07T15:12:37.7790094Z | Promotion | Promoted from `Backlog` to `Plan` after the PRD structure, acceptance criteria, verification table, and next-step gate passed validation. |
| 2026-08-07T15:17:47.6320528Z | Planning decision | Confirmed the compact density contract: remove all Outline entry padding and inter-entry gaps, retain H2/H3 indentation as the only intentional horizontal spacing, and preserve the existing font and line height. |
| 2026-08-07T15:20:02.6399549Z | Promotion | Promoted from `Plan` to `Implement` after the implementation plan, acceptance criteria, verification table, and next-step gate passed validation. |
| 2026-08-07T15:23:34.5024970Z | Scope clarification | Narrowed acceptance and verification to one supported desktop layout; multi-resolution comparison is not required. Functional coverage for density, empty state, navigation, wrapping, active highlighting, and scrolling remains unchanged. |
| 2026-08-07T15:26:18.6885109Z | Implementation | Removed the Outline pane header, changed the Outline container to a single full-height grid row, and removed container, entry, and empty-state padding plus inter-entry gaps. Preserved heading-level indentation, typography, navigation, and state styling. `git diff --check` and `npm run check:workflow` passed. |
| 2026-08-07T15:30:35.4363557Z | Test candidate | Created packaged Test candidate `1.0.7`. `npm run build` produced `src-tauri/target/release/quill-tauri.exe` and `src-tauri/target/release/bundle/nsis/Quill_1.0.7_x64-setup.exe`; the root `quill.exe` was synchronized. SHA-256 for both executables is `8ACE422E7D74B221F7F2A19D047F29E9E1DAECF45B80EB344722DF25DA64F9AA`. `git diff --check`, `npm run check:workflow`, and `node --check` for all 8 JavaScript files passed. Formal visual acceptance remains for `Test`. |
| 2026-08-07T15:35:38.6213385Z | Visual correction | Visual dev check found that the live `outlinePaneTemplate` in `code/quill.html` still contained the removed header, which caused the Outline navigation to occupy an implicit bottom grid row. Removed the duplicate live-template header and restored `6px` vertical padding within each Outline item, with no horizontal padding and no inter-entry gap. The PRD acceptance wording and TC-01 were updated to match the corrected density contract. Candidate `1.0.7` must be superseded by a new patch candidate before Test. |
| 2026-08-07T15:39:37.0071593Z | Test candidate | Created corrected packaged Test candidate `1.0.8`. `npm run build` produced `src-tauri/target/release/quill-tauri.exe` and `src-tauri/target/release/bundle/nsis/Quill_1.0.8_x64-setup.exe`; the root `quill.exe` was synchronized. SHA-256 for both executables is `4593F84DD21E67409CD926529120821CC44B2F230E034627B705C676754886DA`. `git diff --check`, `npm run check:workflow`, all 8 JavaScript syntax checks, and focused static checks for both header-free templates, `6px` item padding, and zero inter-entry gap passed. Formal visual acceptance remains for `Test`. |
