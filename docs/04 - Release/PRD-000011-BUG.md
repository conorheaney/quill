# PRD-000011-BUG

## Short Name

Fix Fenced Markdown Load State

## Goal

Make Quill fully load Markdown files that contain fenced code blocks so the editor, render pane, and outline stay in sync and the app does not show a false file-open failure.

## Context

Quill can currently enter a corrupted half-loaded state when a Markdown file contains fenced code blocks. The source editor updates, but preview rendering throws before the render pane and outline can refresh, and the load flow surfaces a misleading failure toast after partially applying the file content.

## Scope

In:

- reproduce the fenced-code-block load failure against a real Markdown sample
- fix the shared Markdown parsing path so code-fence language normalization is available inside the markdown module
- verify that loading a fenced-markdown document no longer leaves Quill in a split state

Out:

- unrelated Markdown feature expansion
- broader parser redesign
- recent-files UX changes not required for this bug

## Proposed Behavior

- Opening a Markdown file with fenced code blocks should update the source, render pane, and outline together.
- The file-open flow should only show a failure toast when the file truly cannot be opened or parsed.
- Shared Markdown parsing helpers should not depend on private functions scoped to other modules.

## Plan

- Localize language normalization inside the markdown module, verify the shared parser against the fenced-markdown sample, and confirm the real file-open flow no longer leaves the app in a split state.

## Acceptance Criteria

- Parsing Markdown with fenced code blocks no longer throws due to missing language-normalization helpers.
- The supplied `SKILL.md` sample can pass through the shared Markdown parser without error.
- Quill no longer enters a half-loaded state for fenced-markdown files.

## Implementation Notes

- Keep the fix local to the shared markdown module unless verification shows a second fault in the load flow.
- Reuse the existing language normalization mapping so preview editing and file-open parsing stay aligned.
- Bump the versioned `markdown.js` asset URL in `code/quill.html` so direct-browser Chrome sessions pick up the parser fix reliably.

## Verification

- Verified `code/scripts/markdown.js` passes `node --check`.
- Verified `code/quill.html` now points to `./scripts/markdown.js?v=3` so direct-browser reloads can pick up the parser change.
- Verified the supplied `C:\\Projects\\Work\\Research\\.opencode\\skills\\market-research\\SKILL.md` sample now parses through `parseMarkdownBlocks` without throwing.
- Verified the shared parser now returns 76 blocks, including 7 code blocks and 17 headings, for the supplied sample.
- Manually verified the Quill file-open flow after the fix: the Markdown source, render pane, and outline remain aligned for the fenced-markdown scenario that originally failed.

## Next Step

No further implementation work is required for this item.

## Status

Done

## History

| Timestamp | Stage |
| --- | --- |
| 2026-07-12T12:38:02.8092609Z | Backlog |
| 2026-07-12T13:05:37.3879473Z | Plan |
| 2026-07-12T13:05:38.5071839Z | Implement |
| 2026-07-12T13:05:39.6169622Z | Test |
| 2026-07-12T12:47:11.8749153Z | Release |

## Audit

| Timestamp | Type | Detail |
| --- | --- | --- |
| 2026-07-12T12:38:02.8092609Z | Request | User reported that opening `C:\\Projects\\Work\\Research\\.opencode\\skills\\market-research\\SKILL.md` loads the source text but leaves the render and outline panes stale while showing a file-open failure toast. |
| 2026-07-12T13:05:40.7345424Z | Backfill | Added missing `Plan`, `Implement`, and `Test` history rows to restore the required stage sequence under the prior workflow. The original transition times were not captured. |
| 2026-07-12T12:47:11.8749153Z | Manual verification | Promoted to `Release` after manual verification confirmed the fenced-markdown file-open path now keeps the editor, render pane, and outline in sync without the false failure state. |

## Legacy Notes

- This PRD contains legacy non-standard history repair and older untimestamped carryover notes from before the current chronology rule was adopted.
- Legacy root cause note: `parseMarkdownBlocks` in `code/scripts/markdown.js` called `normaliseLanguage`, but that helper was only scoped inside `code/scripts/quill-app.js`, so fenced code blocks threw during preview parsing.
- Legacy plan note: localize language normalization inside the markdown module, then verify the parser against the supplied fenced-markdown sample before promotion.
- Legacy verification note: `node --check` passed for `code/scripts/markdown.js`, and the supplied `SKILL.md` sample parsed successfully with fenced code blocks after the module-local normalization fix.
- Legacy cache-busting note: `code/quill.html` was bumped to `markdown.js?v=3` because direct-browser Chrome sessions in this repo had previously held onto stale renderer assets.
