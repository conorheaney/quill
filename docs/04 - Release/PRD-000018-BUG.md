# PRD-000018-BUG

## Short Name

Fix Relative Image Rendering

## Goal

Make valid Markdown image references render correctly in Quill's application render view.

## Context

Relative image references currently do not render correctly in the rendered Markdown view. This breaks expected Markdown behavior for image content and creates an inconsistency between valid source Markdown and what the application shows to the user.

## Scope

In:

- make valid Markdown image references display in the application render view
- determine how relative image paths should resolve from the currently loaded Markdown document

Out:

- broad media-management features beyond Markdown image rendering
- remote image-policy redesign unless it is required to support valid existing behavior
- unrelated render-pane refactors not needed to restore correct image display

## Plan

- Reproduce the current failure with a concrete relative-path markdown sample, including `![Quill workflow lifecycle](./assets/workflow-lifecycle.png)`, while a markdown file is loaded from disk in the desktop app.
- Confirm how image URLs are currently emitted by the markdown renderer and where relative paths are being resolved today.
- Define desktop rendering so Markdown image sources are resolved relative to the currently loaded markdown document when Quill knows that document's filesystem path.
- Preserve existing support for already-valid absolute and data-image sources unless the image work requires a narrowly scoped sanitizer adjustment.
- Pass the current document location or a resolved base path into the render pipeline so image rendering can resolve relative paths for desktop file-backed documents.
- Keep the implementation slice narrow to markdown image rendering in the preview/render pane; do not add image management, uploads, or unrelated preview refactors.
- Record any decision about path-normalization edge cases in `Audit` if implementation reveals additional within-scope clarification.

## Acceptance Criteria

- In the desktop app, opening a markdown file from disk with a valid relative image reference shows the image in the preview pane when the referenced file exists.
- In the desktop app, an already-supported absolute image source still renders correctly in the preview pane.
- In the desktop app, embedded base64 images still render correctly in the preview pane.

## Verification

- Verify the renderer output with targeted code-level checks around markdown image HTML generation for a relative image path, an absolute `file:` image path, and an existing `data:` image source.
- Verify the relative-path case in the desktop app with a real markdown file plus sibling `assets/` image so the preview pane proves the image renders from disk.
- Confirm the relative-path case resolves from the active markdown document location rather than `window.location.href` alone.
- During test, expected evidence should include preview-visible confirmation for each acceptance criterion plus command output or harness assertions for the renderer-level checks.

## Test Cases

### Test Case 1

| Field | Detail |
| --- | --- |
| Number | `PRD-000018-BUG-TC-01` |
| Title | `Desktop preview renders a valid relative image from disk` |
| Acceptance Criterion | `In the desktop app, opening a markdown file from disk with a valid relative image reference shows the image in the preview pane when the referenced file exists.` |
| Preconditions | A desktop markdown file on disk references an image file that exists at the relative path used in the markdown. |
| Expected Result | The markdown file opens in the desktop app; the preview pane shows the referenced image; the preview does not show broken-image output or raw markdown in place of the image. |
| Date | `2026-07-16T23:15:09.3959827Z` |
| Note | Screenshot evidence captured from the Quill desktop app while `workflow-guide.md` is open, showing the relative markdown image reference in the editor and the rendered workflow image visible in the preview pane. |
| Evidence | [prd-000018-relative-image-tc-01-evidence.png](C:/Users/conor/Documents/Markdown%20Editor/docs/evidence/prd-000018-relative-image-tc-01-evidence.png) |

Steps:
- Launch the Quill desktop app.
- Open a markdown file from disk that contains a valid relative image reference.
- Wait for the preview pane to finish rendering.
- Inspect the preview pane where the image should appear.
- Record whether the image is shown correctly.

### Test Case 2

| Field | Detail |
| --- | --- |
| Number | `PRD-000018-BUG-TC-02` |
| Title | `Desktop preview preserves existing absolute image rendering` |
| Acceptance Criterion | `In the desktop app, an already-supported absolute image source still renders correctly in the preview pane.` |
| Preconditions | A desktop markdown file on disk contains an absolute image source that Quill already supported before this change. |
| Expected Result | The markdown file opens in the desktop app; the preview pane shows the image from the absolute source correctly; no regression is visible in the image rendering path. |
| Date | `2026-07-17T00:34:22.5839474Z` |
| Note | Screenshot evidence captured from the Quill desktop app while the PRD test case is open, showing a fully specified local evidence image path in the markdown pane and the corresponding image rendered correctly in the preview pane. |
| Evidence | [prd-000018-absolute-image-tc-02-evidence.png](C:/Users/conor/Documents/Markdown%20Editor/docs/evidence/prd-000018-absolute-image-tc-02-evidence.png) |

Steps:
- Launch the Quill desktop app.
- Open a markdown file from disk that contains an already-supported absolute image source.
- Wait for the preview pane to finish rendering.
- Inspect the preview pane where the image should appear.
- Record whether the image is shown correctly.

### Test Case 3

| Field | Detail |
| --- | --- |
| Number | `PRD-000018-BUG-TC-03` |
| Title | `Desktop preview preserves embedded base64 image rendering` |
| Acceptance Criterion | `In the desktop app, embedded base64 images still render correctly in the preview pane.` |
| Preconditions | A desktop markdown file on disk contains an embedded base64 image source. |
| Expected Result | The markdown file opens in the desktop app; the preview pane shows the embedded image correctly; no regression is visible in the embedded-image rendering path. |
| Date | `2026-07-17T00:36:24.7900382Z` |
| Note | Screenshot evidence captured from the Quill desktop app showing an embedded base64 image source in the markdown pane and the corresponding rendered image visible in the preview pane. |
| Evidence | [prd-000018-base64-image-tc-03-evidence.png](C:/Users/conor/Documents/Markdown%20Editor/docs/evidence/prd-000018-base64-image-tc-03-evidence.png) |

Steps:
- Launch the Quill desktop app.
- Open a markdown file from disk that contains an embedded base64 image source.
- Wait for the preview pane to finish rendering.
- Inspect the preview pane where the image should appear.
- Record whether the image is shown correctly.

## Next Step

Treat the desktop image-rendering fix as the released baseline for relative paths, fully specified local paths, and embedded base64 images, and use the attached evidence when checking future regressions in the preview pane.

## History

| Timestamp | Stage |
| --- | --- |
| 2026-07-16T20:11:41.6042920Z | Backlog |
| 2026-07-16T20:15:06.9502647Z | Plan |
| 2026-07-16T20:36:32.3243691Z | Implement |
| 2026-07-16T23:11:10.8242040Z | Test |
| 2026-07-16T23:23:17.8228756Z | Implement |
| 2026-07-16T23:52:00.0000000Z | Test |
| 2026-07-17T00:38:11.5438345Z | Release |

## Audit

| Timestamp | Type | Detail |
| --- | --- | --- |
| 2026-07-16T20:11:41.6042920Z | Requirement shaping | Added as a backlog item after clarifying that valid Markdown images should render in the application, and that browser mode should reuse the same behavior where practical or show a placeholder otherwise. |
| 2026-07-16T20:11:41.6042920Z | State | Current state: backlogged and waiting to move into `01 - Plan`. |
| 2026-07-16T20:15:06.9502647Z | Promotion | Promoted from `Backlog` to `Plan` to define relative image path resolution, application render behavior, browser-mode fallback behavior, and the first implementation-ready slice. |
| 2026-07-16T20:20:33.2355745Z | Requirement note | Record the concrete markdown sample `![Quill workflow lifecycle](./assets/workflow-lifecycle.png)` as a case that needs to render as an image. |
| 2026-07-16T20:23:51.4843942Z | Planning detail | Expanded `Plan`, `Acceptance Criteria`, and `Verification` to define desktop relative-path resolution from the active markdown document, browser-mode placeholder expectations when file context is unavailable, and the required regression and evidence checks. |
| 2026-07-16T20:36:32.3243691Z | Promotion | Promoted from `Plan` to `Implement` after confirming the planning sections were detailed enough to guide the image-path work, and updated `Next Step` to point at the concrete implementation slice. |
| 2026-07-16T20:43:45.8017331Z | Implementation | Added a shared markdown render context so preview image URLs can resolve relative paths from the active markdown document path, and render a visible placeholder when the session lacks a safe filesystem base path for relative images. |
| 2026-07-16T20:43:45.8017331Z | Verification | Verified with `node --check` that `code/scripts/markdown.js`, `code/scripts/quill-app.js`, and `code/scripts/preview-pane.js` all parse successfully; verified in a Node VM harness that `![Quill workflow lifecycle](./assets/workflow-lifecycle.png)` resolves to a `file:///.../assets/workflow-lifecycle.png` image when a document base URL is present, falls back to a preview placeholder without that base URL, and preserves existing `data:` and absolute `file:` image rendering. |
| 2026-07-16T20:43:45.8017331Z | Verification gap | Confirmed the localhost smoke page served successfully on `http://127.0.0.1:1420/smoke-check.html`, but a browser-visible automation pass is still pending because Playwright's Chromium executable is not installed in the local runtime on 2026-07-16. |
| 2026-07-16T20:57:26.0834980Z | Implementation | Reworked desktop local-image rendering so Quill resolves markdown image references to local filesystem paths and hydrates them through a dedicated Tauri `read_image_data_url` command, instead of relying on raw `file:///` image URLs inside the webview. |
| 2026-07-16T20:57:26.0834980Z | Verification | Verified with `cargo check` that the Tauri shell still compiles after adding the desktop image-read command, and verified in a Node VM harness against `.scratch/workflow-guide.md` that Quill now resolves both sample image references to `C:\\Users\\conor\\Documents\\Markdown Editor\\.scratch\\assets\\workflow-lifecycle.png` and `C:\\Users\\conor\\Documents\\Markdown Editor\\.scratch\\assets\\workflow-tracking-model.png`. |
| 2026-07-16T23:11:10.8242040Z | Promotion | Promoted from `Implement` to `Test` after agreeing that the remaining preview-visible checks will be completed during manual testing, and updated `Next Step` to capture that manual verification and closeout path explicitly. |
| 2026-07-16T23:14:41.0683434Z | Testability | Tightened the acceptance criteria into human-testable pass/fail checks so the remaining manual verification can be run directly in Quill without needing code-level inspection. |
| 2026-07-16T23:32:16.5377845Z | Scope split | Removed the browser-specific acceptance criteria after deciding to track browser-mode removal as a separate follow-on change instead of keeping that behavior inside this bug item's acceptance boundary. |
| 2026-07-16T23:35:08.3116764Z | Verification detail | Expanded the verification section so each remaining acceptance criterion has one matching test case plus the supporting renderer-level evidence expected during Test. |
| 2026-07-16T23:40:00.0000000Z | Test case formatting | Reworked the acceptance-test coverage into a dedicated `Test Cases` section so the location and structure match the released `PRD-000017-TECH` format. |
| 2026-07-16T23:15:09.3959827Z | Test evidence | Registered screenshot evidence for `PRD-000018-BUG-TC-01` in `docs/evidence/prd-000018-relative-image-tc-01-evidence.png`, showing the relative markdown image reference and the rendered image visible in the Quill desktop preview pane. |
| 2026-07-16T23:23:17.8228756Z | Test failure | Demoted from `Test` back to `Implement` after manual testing showed fully specified local image paths still do not render correctly, while relative paths and online URLs do render correctly. |
| 2026-07-16T23:48:00.0000000Z | Implementation | Updated desktop local-image path handling so fully specified Windows filesystem image paths are percent-decoded before preview hydration, allowing hardcoded paths with escaped spaces such as `%20` to resolve to the real on-disk file. |
| 2026-07-16T23:48:00.0000000Z | Verification | Verified with `node --check` that `code/scripts/markdown.js` still parses, and verified in a Node VM harness that a fully specified local image reference using `C:/Users/conor/Documents/Markdown%20Editor/docs/evidence/prd-000012-table-render-fix-evidence.png` now renders to an `img` element with `data-local-image-path="C:\\Users\\conor\\Documents\\Markdown Editor\\docs\\evidence\\prd-000012-table-render-fix-evidence.png"`. |
| 2026-07-16T23:52:00.0000000Z | Promotion | Promoted from `Implement` to `Test` after the fully specified local-path fix landed and renderer-level verification showed percent-escaped hardcoded filesystem paths now normalize to a valid desktop preview image path. |
| 2026-07-17T00:34:22.5839474Z | Test evidence | Registered screenshot evidence for `PRD-000018-BUG-TC-02` in `docs/evidence/prd-000018-absolute-image-tc-02-evidence.png`, showing a fully specified local image path in the markdown pane and the corresponding image rendered correctly in the Quill desktop preview pane. |
| 2026-07-17T00:36:24.7900382Z | Test evidence | Registered screenshot evidence for `PRD-000018-BUG-TC-03` in `docs/evidence/prd-000018-base64-image-tc-03-evidence.png`, showing an embedded base64 image source in the markdown pane and the corresponding image rendered correctly in the Quill desktop preview pane. |
| 2026-07-17T00:38:11.5438345Z | Promotion | Promoted from `Test` to `Release` after the desktop verification set was completed with attached evidence covering relative image rendering, fully specified local image rendering, and embedded base64 image rendering. |
