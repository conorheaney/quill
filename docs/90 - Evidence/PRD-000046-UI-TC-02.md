# TC-02 Evidence

| Field | Detail |
| --- | --- |
| PRD | [PRD-000046-UI](../../docs/25%20-%20Closed/PRD-000046-UI.md) |
| Acceptance Criteria | AC-02 |
| Product Version | 1.1.2 |
| Status | complete |
| Recorded | 2026-08-31T11:14:40.0047119Z |
| Test | Automate rejected startup initialization and verify the failed overlay and retry behavior. |
| Result | PASS |

## Preconditions

- Use the committed Quill 1.1.2 source.
- Provide the startup controller's focused automated harness with a minimal DOM fixture containing the overlay, loading message, error message, and retry button.
- Provide a test window with a reload spy and a controlled rejected initialization promise.

## Steps to Reproduce

1. Run the focused startup-failure harness against `frontend/scripts/startup-controller.ts` with the DOM fixture and reload spy.
2. Invoke `window.QuillStartup.showFailure()` in the harness.
3. Assert the seven focused outcomes: the overlay state is `failed`; the loading message is hidden; the actionable error is visible; the retry control is visible; the retry control has focus; retry requests an application reload; and the initialization-rejection boundary calls `showFailure()`.
4. Run the automated background suite with `npm test`.

## Expected Results

- The focused harness reports PASS for all seven AC-02 outcomes listed in step 3.
- The background Node test layer reports 47 passed and 0 failed tests.
- The background Rust test layer reports 8 passed and 0 failed tests.

## Evidence

The focused automated harness passed all assertions for the failed overlay state, message visibility, retry visibility and focus, and reload behavior. The relevant production code is captured below.

`frontend/scripts/startup-controller.ts`

```ts
function showFailure(): void {
  if (!overlay) return;
  overlay.dataset.state = "failed";
  overlay.removeAttribute("aria-hidden");
  if (message) message.hidden = true;
  if (error) error.hidden = false;
  if (retry) {
    retry.hidden = false;
    retry.addEventListener("click", () => window.location.reload(), { once: true });
    retry.focus();
  }
}
```

`frontend/scripts/quill-app.ts`

```ts
initialiseQuill().catch((error: unknown) => {
  console.error("Unable to initialise Quill", error);
  window.QuillStartup.showFailure();
});
```

Captured automated results for each AC-02 assertion:

```text
1. Overlay state: failed [PASS]
2. Loading message hidden: true [PASS]
3. Actionable error visible: true [PASS]
4. Retry control visible: true [PASS]
5. Retry control focused: true [PASS]
6. Retry requests reload: true [PASS]
7. Initialization rejection calls showFailure: true [PASS]
TC-02 detailed result: PASS
```

The repository background suite also passed: Node test layer 47 passed and 0 failed; Rust test layer 8 passed and 0 failed.
