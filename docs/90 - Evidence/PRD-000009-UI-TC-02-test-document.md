# Theme Comparison Test Document

Use this document for TC-02 to compare the Light, Dark, Sepia, and Nord themes against the same visible content.

## Theme comparison checklist

- [ ] Light selected and observed.
- [ ] Dark selected and observed.
- [ ] Sepia selected and observed.
- [ ] Nord selected and observed.
- [ ] Each theme has a visibly distinct appearance.

## Document content

### A short heading

Quill is a local-first Markdown editor for writing, reviewing, and refining documents.

This paragraph provides regular body text for comparing text contrast, panel surfaces, spacing, and the rendered preview.

### Key points

- Theme choices should apply immediately.
- The document should remain readable in every theme.
- The preview should preserve this Markdown structure.

### Ordered steps

1. Open the theme chooser.
2. Select a named theme.
3. Compare the application and preview appearance.

### Example code

```javascript
const themeChoices = ["Light", "Dark", "Sepia", "Nord"];
const selectedTheme = themeChoices[0];
console.log(`Selected theme: ${selectedTheme}`);
```

### Inline formatting

This sentence includes **strong text**, *emphasized text*, and `inline code` for checking contrast and readability.

### Quote

> A consistent visual theme should make the document easier to read without changing its content.

### Comparison table

| Surface | Expected content |
| --- | --- |
| Editor | Markdown source remains readable. |
| Preview | Formatted content remains readable. |
| Toolbar | Theme chooser remains visible and usable. |

### Final check

The text, headings, list structure, code block, quote, and table should remain unchanged while switching between all four themes.
