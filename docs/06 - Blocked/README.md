# Blocked PRDs

This folder holds PRDs whose work is blocked. A blocked PRD has no row in `BACKLOG.md`; its last active lifecycle phase remains recorded in `History`, and the blocking condition is recorded in `Audit`.

When unblocked, return the PRD to the phase recorded in `History` and resume the normal lifecycle. Do not advance a blocked PRD while it is held here.
