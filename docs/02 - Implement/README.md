# Implement Stage

Use this folder for backlog items that are actively being built and need working notes.

Typical content:

- implementation approach
- in-flight decisions
- partial progress notes
- blockers discovered during development

Rules:

- only active implementation items belong here
- prefer one active item at a time
- move the file to `docs/03 - Test/` when implementation is ready for verification
