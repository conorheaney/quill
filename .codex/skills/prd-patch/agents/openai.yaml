interface:
  display_name: "PRD Test Candidate"
  short_description: "Prepare committed PRD Test candidates safely"
  default_prompt: "Use $prd-patch to bump the patch version, build the candidate, synchronize product.exe, verify scope, and stop before PRD promotion."
