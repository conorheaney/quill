interface:
  display_name: "PRD Backlog"
  short_description: "Create shaped project backlog items safely."
  default_prompt: "Use $prd-backlog to shape and create a valid project backlog item with its initial PRD."
