interface:
  display_name: "PRD Promote"
  short_description: "Promote project PRDs between workflow phases."
  default_prompt: "Use $prd-promote to validate and promote a project PRD to its next allowed workflow phase."
