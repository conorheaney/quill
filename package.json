{
  "name": "Quill",
  "version": "1.0.9",
  "description": "Local-first Markdown editor with a Tauri desktop shell.",
  "private": true,
  "scripts": {
    "start": "npm run tauri:dev",
    "smoke:dev": "node scripts/serve-quill.mjs",
    "check:workflow": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/check-workflow.ps1",
    "check:workflow:details": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/check-workflow.ps1 -Detailed",
    "tauri:info": "tauri info",
    "tauri:dev": "tauri dev",
    "version:bump": "node scripts/bump-version.mjs",
    "build": "npm run tauri:build",
    "tauri:build": "tauri build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "@tauri-apps/plugin-dialog": "^2.7.1"
  }
}
